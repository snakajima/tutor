rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /words/{wordId} {
      allow read;
      allow delete, update, create: if false;
    }
    match /{allPaths=**} {
      allow read, write: if request.auth!=null;
    }
  }
}
